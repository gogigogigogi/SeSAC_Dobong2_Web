<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>실습문제1</title>
  </head>
  <body>
    <h1>실습문제1</h1>
    <form
      action="/practice_result/post"
      method="post"
      onsubmit="return checkForm(this)"
      target="_blank"
    >
      <label for="name">이름</label>
      <input type="text" id="name" name="name" />

      <fieldset>
        <legend>성별</legend>
        <label>
          <input type="radio" name="gender" value="남자" />
          남자
        </label>
        <label>
          <input type="radio" name="gender" value="여자" />
          여자
        </label>
      </fieldset>
      <fieldset>
        <legend>생년월일</legend>
        <select name="year">
          <% for(let i=1970; i<2025; i++){%>
          <option><%=i%></option>
          <%}%>
        </select>
        년
        <select name="month">
          <% for(let i=1; i<13; i++){%>
          <option><%=i%></option>
          <%}%>
        </select>
        월
        <select name="day">
          <% for(let i=1; i<32; i++){%>
          <option><%=i%></option>
          <%}%>
        </select>
        일
      </fieldset>
      <fieldset>
        <legend>관심사</legend>
        <label>
          <input type="checkbox" name="hobby" value="여행" />
          여행
        </label>
        <label>
          <input type="checkbox" name="hobby" value="패션" />
          패션
        </label>
        <label>
          <input type="checkbox" name="hobby" value="음식" />
          음식
        </label>
      </fieldset>
      <fieldset>
        <legend>선택</legend>
        <label>
          좋아하는 색상
          <input type="color" name="color" />
        </label>
        <br />
        <label>
          좋아하는 숫자
          <input type="number" name="number" v />
        </label>
      </fieldset>
      <button type="submit">회원가입</button>
    </form>
    <script>
      function checkForm(form) {
        if (form.name.value.trim() === "" || !form.gender.value) {
          alert("이름과 성별 모두 입력하세요");
          return false;
        }
      }
    </script>
  </body>
</html>
